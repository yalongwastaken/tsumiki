# ── dependencies ─────────────────────────────────────────────
node_modules/

# ── build output ─────────────────────────────────────────────
client/dist/
client/test/.tmp/
*.timestamp-*.mjs
.vite/

# ── SQLite data + backups (never commit personal finance data) ─
server/data/
*.db
*.db-wal
*.db-shm
backups/

# ── local sample data (smoke-test fixtures, never committed) ─
/samples/
sample*

# ── logs / env ───────────────────────────────────────────────
*.log
npm-debug.log*
.env
.env.*

# ── python venv for the optional price-sync sidecar ──────────
.venv/

# ── editor / OS / FUSE cruft ─────────────────────────────────
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db
.fuse_hidden*
__pycache__/
