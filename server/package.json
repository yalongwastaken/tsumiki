{
  "name": "tsumiki-server",
  "version": "2.2.0",
  "private": true,
  "type": "module",
  "description": "Personal money-coach backend (mini-PC brain). Express + node:sqlite.",
  "scripts": {
    "start": "node --experimental-sqlite index.js",
    "dev": "node --experimental-sqlite --watch index.js",
    "test": "node --experimental-sqlite --test test/*.test.js"
  },
  "engines": {
    "node": ">=22.12"
  },
  "dependencies": {
    "express": "^4.21.0"
  }
}
